import React from 'react';
import {Box} from "@chakra-ui/react";

const Header = () => {
    return (
        <Box height={20} 
             bgColor="green">
          Test  
        </Box>
    );
};

export default Header;